
MongoDB Schema



Paste => {
    'schemaVer': Int,
    'downloadTime': ts,
    'url': URL,
    'site': Site,
    'content': Text,
    'tags': list of tags
    'alertedOn': string alerted on
    'site' : pastesite
}


MongoDB

http://docs.mongodb.org/manual/administration/security/

Use 'auth' setting in /usr/local/etc/mongod.conf

Add instance for (DB.Val ..) for all data in DB

